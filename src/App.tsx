function App() {
	return (
		<main className="min-h-svh flex items-center justify-center p-8">
			<div className="text-center">
				<h1 className="text-4xl font-semibold tracking-tight">
					Neighbourhood Pulse
				</h1>
				<p className="mt-2 text-muted-foreground">
					Vite + React + Fastify + AI SDK
				</p>
			</div>
		</main>
	);
}

export default App;
